When we inspected the form on the survey page, we saw that we could change grade values,
so we changed a value above 10 and then we submitted the form

To prevent this, you can check if a value is in the range of the expected values